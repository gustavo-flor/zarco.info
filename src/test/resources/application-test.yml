datasource:
  read-write:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: test
    password: test
    driver-class-name: org.h2.Driver
  read-only:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: test
    password: test
    driver-class-name: org.h2.Driver
    hikari:
      read-only: true
